groups:
  - name: master
    jobs:
      - development
      - qa
      - integration
  - name: pull-request
    jobs:
      - dataworks-aws-ingest-replica-pr
  - name: update-pipeline
    jobs:
      - update-pipeline
  - name: utility
    jobs:
      - start-cluster-dev
      - terminate-replica
