groups:
  - name: master
    jobs:
      - development
      - qa
      - integration
      - preprod
  - name: pull-request
    jobs:
      - dataworks-aws-ingest-replica-pr
  - name: update-pipeline
    jobs:
      - update-pipeline
